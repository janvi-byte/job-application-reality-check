# System Architecture

The project follows a simple modular structure.

-> main.py handles user input
-> evaluation.py handles logic and decision-making
-> docs folder contains explanations and design thinking

Flow:
User Input → Skill Evaluation → Readiness Decision → Guidance Output
